/**
 * Enum representing types of email sends.
 */
export enum EMAIL_SEND_TYPE {
  /**
   * Represents the email send type for resetting a password.
   */
  RESET_PASSWORD = 'RESET_PASSWORD',

  /**
   * Represents the email send type for email verification.
   */
  EMAIL_VERIFICATION = 'EMAIL_VERIFICATION'
}
